[package]
name = "flowlink-integrations-webhook"
version = "0.1.0"
edition = "2021"
description = "FlowLink Webhook integration — forward events to any HTTP endpoint"

[dependencies]
async-trait = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.12", features = ["json", "rustls-tls"], default-features = false }
log = "0.4"
anyhow = "1"
hmac = "0.12"
sha2 = "0.10"
hex = "0.4"
chrono = { version = "0.4", features = ["serde"] }
flowlink-integrations-core = { path = "../integrations-core" }

[features]
default = []
