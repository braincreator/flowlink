[package]
name = "flowlink-shield"
version = "0.3.4"
edition = "2021"
description = "FlowLink Shield — eBPF-powered 3-level command guardian"

[dependencies]
flowlink-core = { path = "../core" }
tokio = { version = "1", features = ["full", "sync"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
log = "0.4"
anyhow = "1"
clap = { version = "4", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }
regex = "1"
once_cell = "1"
reqwest = { version = "0.12", features = ["json"] }
libc = "0.2"
tree-sitter = "0.24"
tree-sitter-bash = "0.23"
axum = "0.8"
dashmap = "6"
prometheus = "0.13"
serde_yaml = "0.9"

[dev-dependencies]
criterion = "0.5"
tempfile = "3"
tokio = { version = "1", features = ["rt", "macros", "test-util"] }
tower = "0.5"

[[bench]]
name = "bench_shield"
harness = false

[features]
ebpf = ["aya"]
gitops = ["flowlink-gitops"]

[dependencies.aya]
version = "0.12"
optional = true

[dependencies.flowlink-gitops]
path = "../gitops"
optional = true
