[advisories]
# rsa (RUSTSEC-2023-0071) comes via sqlx-mysql which is pulled in by
# sqlx-macros-core at compile time even though FlowLink only uses PostgreSQL.
# The rsa code is never linked into the final binary (mysql feature disabled).
# No fix is available upstream.
ignore = ["RUSTSEC-2023-0071"]
